apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-admin-ingress
spec:
  podSelector:
    matchLabels:
      'app.kubernetes.io/name': admin
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - {}
  egress:
    - {}